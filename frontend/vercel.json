{
    "routes": [{ "src": "/[^.]+", "dest": "/", "status": 200 }],
    "builds": [
      {
        "src": "dist/**/*.+(js|css|json|txt|ico|svg|png|jpg|jpeg)",
        "use": "@vercel/static"
      }
    ]
  }